# Set this in your host's environment settings (Vercel/Netlify dashboard),
# not in a committed file. It must be an https:// URL: a page served over
# https cannot call an http:// backend -- browsers block it as mixed content.
VITE_API_BASE_URL=https://your-backend.onrender.com
